import type { Meta, StoryObj } from '@storybook/react';
import {{ name }} from './{{ name }}';

export default {
  title: 'Components/{{ storybookTitle }}',
  component: {{ name }},
} satisfies Meta<typeof {{ name }}>;

type Story = StoryObj<typeof {{ name }}>;

export const Default: Story = {
  args: {

  },
};
