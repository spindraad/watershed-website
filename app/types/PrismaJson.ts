declare global {
  // eslint-disable-next-line @typescript-eslint/no-namespace
  namespace PrismaJson {
    type Localised = {
      nl: string;
      en: string;
      pap: string;
    };
  }
}

export {};
