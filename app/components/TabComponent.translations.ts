// TabComponent doesn't use translations directly since content is passed as props
// This file is kept for consistency with the component structure pattern

export const en = {
  TabComponent: {},
};

export const nl = {
  TabComponent: {},
};

export const pap = {
  TabComponent: {},
};
