name: Database migrations
on:
  push:
    paths:
      - prisma/migrations/**
    branches:
      - main
      - dev

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Here is where the migration will be applied"
#      - name: Checkout repo
#        uses: actions/checkout@v4
#      - name: Setup Node
#        uses: actions/setup-node@v4
#      - name: Install dependencies
#        run: npm install
#      - name: Apply all pending migrations to the database
#        run: npx prisma migrate deploy
#        env:
#          DATABASE_URL: ${{ github.base_ref == 'main' && secrets.DATABASE_URL_PROD || secrets.DATABASE_URL_DEV }}
