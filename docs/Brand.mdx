import { Meta, ColorPalette, ColorItem, Typeset } from '@storybook/blocks';
import {
  purple,
  peach,
  darkGrey,
  peachRed,
  foamSpray,
  yellow,
  screaminGreen,
} from './colors';

<Meta title="Brand" />

# Brand

The Watershed brand is currently being redesigned. This page will be updated with the new brand guidelines once they are available.

## Surface

The Watershed brand uses a light surface color for its backgrounds.

<ColorPalette>
  <ColorItem
    title="Surface"
    subtitle="Default"
    colors={['rgb(var(--surface__surface))']}
  />
</ColorPalette>

<ColorPalette>
  <ColorItem
    title="Surface"
    subtitle="Primary"
    colors={['rgb(var(--surface__primary-container))']}
  />
</ColorPalette>

<ColorPalette>
  <ColorItem
    title="Surface"
    subtitle="Secondary"
    colors={['rgb(var(--surface__secondary-container))']}
  />
</ColorPalette>

## Colors

Watershed currently uses the following colors in its branding:

### Purple

<ColorPalette>
  {Object.keys(purple).map((shade) => (
    <ColorItem
      key={shade}
      title="Purple"
      subtitle={`${shade}`}
      colors={[`rgb(var(${shade}))`]}
    />
  ))}
</ColorPalette>

### Peach

<ColorPalette>
  {Object.keys(peach).map((shade) => (
    <ColorItem
      key={shade}
      title="Peach"
      subtitle={`${shade}`}
      colors={[`rgb(var(${shade}))`]}
    />
  ))}
</ColorPalette>

### Dark Grey

<ColorPalette>
  {Object.keys(darkGrey).map((shade) => (
    <ColorItem
      key={shade}
      title="Dark Grey"
      subtitle={`${shade}`}
      colors={[`rgb(var(${shade}))`]}
    />
  ))}
</ColorPalette>

### Peach Red

<ColorPalette>
  {Object.keys(peachRed).map((shade) => (
    <ColorItem
      key={shade}
      title="Peach Red"
      subtitle={`${shade}`}
      colors={[`rgb(var(${shade}))`]}
    />
  ))}
</ColorPalette>

### Foam Spray

<ColorPalette>
  {Object.keys(foamSpray).map((shade) => (
    <ColorItem
      key={shade}
      title="Foam Spray"
      subtitle={`${shade}`}
      colors={[`rgb(var(${shade}))`]}
    />
  ))}
</ColorPalette>

### Yellow

<ColorPalette>
  {Object.keys(yellow).map((shade) => (
    <ColorItem
      key={shade}
      title="Yellow"
      subtitle={`${shade}`}
      colors={[`rgb(var(${shade}))`]}
    />
  ))}
</ColorPalette>

### Screamin' Green

<ColorPalette>
  {Object.keys(screaminGreen).map((shade) => (
    <ColorItem
      key={shade}
      title="Screamin' Green"
      subtitle={`${shade}`}
      colors={[`rgb(var(${shade}))`]}
    />
  ))}
</ColorPalette>

## Fonts

Currently, Arial and regular `sans-serif` are used as the font for the Watershed brand.

<Typeset
  fontFamily="var(--font-family-sans)"
  fontSizes={[
    'var(--sl-font-size-2x-small)',
    'var(--sl-font-size-x-small)',
    'var(--sl-font-size-small)',
    'var(--sl-font-size-medium)',
    'var(--sl-font-size-large)',
    'var(--sl-font-size-x-large)',
    'var(--sl-font-size-2x-large)',
    'var(--sl-font-size-3x-large)',
    'var(--sl-font-size-4x-large)',
  ]}
/>
